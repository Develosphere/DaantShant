# Install from repo root:
#   pip install -r requirements.txt -c constraints.txt
#
# If teeth-analyzer crashes on import (numpy/cv2), run:
#   .\scripts\fix-numpy-opencv.ps1
-c constraints.txt

# ── Editable workspace packages ──────────────────────────────────────────────
-e ./packages/dantshaant_common
-e ./services/teeth_analyzer
-e ./services/diagnosis
-e ./orchestrator

# ── Orchestrator extras (portal auth, RAG, LangGraph recommendation agent) ───
# Also declared in orchestrator/pyproject.toml — listed here for one-shot installs.
motor>=3.3.0
pymongo>=4.6.0
bcrypt>=4.0.0
PyJWT>=2.8.0
email-validator>=2.0.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
PyPDF2>=3.0.0
python-docx>=0.8.11
langgraph>=0.2.40
langchain-core>=0.3.0
